@model KMITL_WebDev_MiniProject.DTO.ActivityDTO

@{
    ViewData["Title"] = Model?.Act.Name ?? "Activity Details";
}

<div class="activity-grid">
    <div class="card">
        <div class="card-header">
            <div class="detail">
                <h3>@Model?.Act.Name</h3>
                <p>@Model?.Act.Description</p>
                <div class="activity-info">
                    <p><strong>Event Date:</strong> @Model?.Act.EventDate.ToString("MMMM dd, yyyy")</p>
                    <p><strong>Location:</strong> @Model?.Act.Location</p>
                    <p><strong>Max Participants:</strong> @Model?.Act.MaxPeople</p>
                    @if (!string.IsNullOrEmpty(Model?.Act.MapUrl))
                    {
                        <p><strong>Map URL:</strong> <a href="@Model..Act.apUrl" target="_blank">View on Map</a></p>
                    }
                    <p><strong>Recruiting Mode:</strong> 
                        @if (Model?.Act.RecruitingMode == 1)
                        {
                            <span>First Come, First Served</span>
                        }
                        else if (Model?.Act.RecruitingMode == 2)
                        {
                            <span>Random on Event Day</span>
                        }
                        else if (Model?.Act.RecruitingMode == 3)
                        {
                            <span>Owner Selection</span>
                        }
                    </p>
                    <p><strong>Show Participants:</strong> @(Model?.Act.ShowParticipants == true ? "Yes" : "No")</p>
                </div>
            </div>
            <div class="card-image">
                @if (!string.IsNullOrEmpty(Model?.Act.ImageUrl))
                {
                    <img src="@Model..Act.mageUrl" alt="@Model?.Act.Name" style="width:100%; border-radius:10px; max-height: 300px; object-fit: cover;">
                }
                else
                {
                    <div style="background-color: #f0f0f0; height: 300px; border-radius: 10px; display: flex; align-items: center; justify-content: center;">
                        <span style="color: #999;">No Image</span>
                    </div>
                }
                <button class="join-btn">Join!</button>
            </div>
        </div>
        <div class="card-body">
            <div class="user-meta">Activity Owner</div>
            <p><strong>Created:</strong> @Model?.Act.CreatedAt.ToString("MMM dd, yyyy")</p>
            <p><strong>Last Updated:</strong> @Model?.Act.UpdatedAt.ToString("MMM dd, yyyy")</p>
        </div>
    </div>

    <div>
        <label>Comment</label>
        <input type="text" id="create-comment" act-id="@Model.Act.Id"/>
    </div>
    
    <section id="comment-section">
        @await Html.PartialAsync("~/Views/Activity/Comment.cshtml", Model.Comments)
    </section>
</div>

<style>
    .activity-grid {
        display: grid;
        grid-template-columns: repeat(auto-fill, max(1000px, 1fr));
        gap: 200px;
    }

    .card {
        padding: 15px;
    }

    .detail {
        min-width: 100px;
        word-wrap: break-word;
        overflow-wrap: break-word;
    }

    .activity-info {
        margin-top: 15px;
        line-height: 1.8;
    }

    .activity-info p {
        margin: 8px 0;
    }

    .card-header {
        display: flex;
        justify-content: space-between;
        align-items: flex-start;
        width: 100%;
    }

    .card-image {
        max-width: 350px;
        flex-shrink: 0;
    }

    .join-btn {
        background: #FF7061;
        color: white;
        border: none;
        padding: 15px 50px;
        border-radius: 10px;
        cursor: pointer;
        margin-top: 15px;
        width: 100%;
        font-size: 16px;
        transition: background-color 0.2s;
    }

    .join-btn:hover {
        background: #e85d52;
    }
</style>

<script src="~/js/Activity/ActivityDetail.js"></script>